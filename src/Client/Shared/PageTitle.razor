@inject TitleManager TitleManager
<!--<title>@Title</title>-->

@code {
    [Parameter] public string? Title { get; set; }
    [Parameter] public bool HidePrimary { get; set; }

    protected override void OnParametersSet()
    {
        TitleManager.Update(Title, HidePrimary);
    }
}
