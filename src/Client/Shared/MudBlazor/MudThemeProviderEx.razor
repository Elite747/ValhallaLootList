@*Removes MudPopoverProvider from MudThemeProvider. Allows server pre-rendering to occur with theming support.*@
@namespace MudBlazor
@inherits BaseMudThemeProvider

@if(!DefaultScrollbar)
{
    @((MarkupString)BuildMudBlazorScrollbar())
}

@*Needed for charts, maybe put in bool, IncludeCharts="true"??*@
<style>
    .mud-chart-serie:hover {
        filter: url(#lighten);
    }
</style>

@((MarkupString)BuildTheme())